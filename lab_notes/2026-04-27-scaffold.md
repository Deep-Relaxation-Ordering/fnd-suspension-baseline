# 2026-04-27 — Repo scaffold

*Endorsement Marker: Local stewardship — U. Warring, AG Schätz, Physikalisches
Institut Freiburg.*

## Context

Initial scaffold of `fnd-suspension-baseline`, the implementation repo for
the breakout note
[*Numerical Pilot: Brownian Motion and Sedimentation of Diamond Particles in
Aqueous Suspension as a Function of Particle Size*](https://github.com/Deep-Relaxation-Ordering/diamonds_in_water/blob/main/breakout-note-brownian-sedimentation.md)
(v0.2 with round-3 + round-4 + cd-rules-reference follow-ups, currently
under review in PR #2 against `Deep-Relaxation-Ordering/diamonds_in_water`).

## What was done

- Created the directory tree per breakout-note §4.3, with cd-rules §14
  additions:
    - `README.md`, `LICENCE` (MIT), `pyproject.toml`, `.gitignore`
    - `src/{parameters,analytical,langevin,fokker_planck,regime_map}.py`
      — all stubs with docstrings cross-referencing the breakout note.
    - `tests/test_*.py` — one module per validation check from §4.4.
      Each test is `pytest.mark.skip` until its underlying implementation
      lands.
    - `notebooks/.gitkeep` — populated as deliverables 2–4 are produced.
    - `lab_notes/2026-04-27-scaffold.md` — this note.
    - `docs/conventions.md` — pinned cd-rules at commit
      `ee01c80352dd8446f189c3159a3d9e347463902c` (2026-04-17), with a
      per-rule applicability table.
    - `archive/README.md` — explains cd-rules §0.8 deprecation policy.

## Decisions

| Decision | Rationale |
|---|---|
| Sibling location at `~/Documents/GitHub/fnd-suspension-baseline` | Independent repo, conventional layout; the breakout note (spec) and this repo (artefact) are separable. |
| British `LICENCE` spelling (not `LICENSE`) | cd-rules §14 uses `LICENCE` consistently. PEP 639 / modern setuptools accept arbitrary licence-file names. |
| Flat `src/` layout, not `src/<package>/` | Matches the breakout-note §4.3 sketch literally. Tests find modules via `pyproject.toml` `[tool.pytest.ini_options] pythonpath = ["src"]`. |
| Skip `index.html` and `assets/` from cd-rules §14 standard tree | This is a numerical-method repo with no web-specimen surface. Documented in README. |
| Pin cd-rules at commit `ee01c80…` (current `main`) rather than the local clone at `8671c93` | Remote `main` is authoritative per cd-rules §0.12. |

## What is *not* done

- No methods are implemented. All `tests/test_*.py` are skipped.
- No GitHub remote yet; awaiting user authorisation for repo creation
  under `Deep-Relaxation-Ordering` (or another) organisation.
- The breakout note's pinned commit hash is left blank in
  `docs/conventions.md` until PR #2 merges.

## Next session

Phase 1 of the breakout-note timeline (§9): implement `parameters.py`
(SI constants, water properties), wire up the first concrete tests in
`test_water_properties.py` and `test_einstein_relation.py`, and replace
the `pytest.mark.skip` on those with real assertions. Effort estimate
1–2 days.
